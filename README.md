# DOM (Document Object Model)

The **Document Object Model (DOM)** is a programming interface that represents a web page as a structured tree.  
It allows JavaScript to dynamically access, modify, and manipulate HTML elements, content, and styles.

## Topics Covered
- DOM Structure
- Selecting Elements
- Modifying Content
- Event Handling

## Purpose
This repository is created to understand and practice DOM concepts using JavaScript.
